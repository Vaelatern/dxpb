usage: dxpb-poke [arguments]

Arguments:
  -h		This help, and exit
  -L		Print license information and exit
  -i <endpoint>	ZeroMQ endpoint for connecting to the import agent

The following flags have defaults.
  -i: ipc:///var/run/dxpb/pkgimport.sock
